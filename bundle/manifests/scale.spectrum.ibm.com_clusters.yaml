apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  creationTimestamp: null
  labels:
    app.kubernetes.io/instance: ibm-spectrum-scale
    app.kubernetes.io/name: operator
  name: clusters.scale.spectrum.ibm.com
spec:
  group: scale.spectrum.ibm.com
  names:
    categories:
    - scale
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    shortNames:
    - gpfs
    singular: cluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.license.license
      name: Edition
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Cluster is the Schema for the clusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Cluster
            properties:
              csi:
                description: csi defines the configuration of the Container Storage
                  Interface (CSI).
                properties:
                  containerResources:
                    description: |-
                      containerResources allow to specify memory and CPU limits for containers of CSI pods.
                      In large clusters with high number of PersistentVolumes the CSI containers require more resources than default values.
                      Only configure this setting if advised by IBM.
                    items:
                      properties:
                        name:
                          description: name is the container name.
                          enum:
                          - driver
                          - sidecar
                          type: string
                        resources:
                          description: resources defines the resources for this container
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This field depends on the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                      required:
                      - name
                      - resources
                      type: object
                    type: array
                  sidecar:
                    description: sidecar specifies configuration options for the CSI
                      sidecar pods
                    properties:
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: nodeSelector is the label selector for CSI sidecar
                          pods. Nodes that do not run an IBM Storage Scale core pod
                          are ignored for scheduling CSI sidecar pods.
                        type: object
                    type: object
                type: object
              daemon:
                default:
                  nsdDevicesConfig:
                    bypassDiscovery: true
                description: It tells the operator how to configure the gpfs daemons.
                properties:
                  clusterNameOverride:
                    description: clusterName overrides the default name, which is
                      that of the Daemon resource itself postpended with any resolved
                      domain suffix.
                    maxLength: 115
                    pattern: ^(?:(?:[a-z0-9](?:[\-a-z0-9]*[a-z0-9])?)\.?)+$
                    type: string
                  clusterProfile:
                    description: |-
                      IBM Storage Scale configuration parameters for the cluster.
                      Changing these values is unsupported and should
                      not be changed unless advised by IBM Support
                    properties:
                      afmAsyncDelay:
                        type: string
                      afmDIO:
                        type: string
                      afmHashVersion:
                        type: string
                      afmMaxParallelRecoveries:
                        type: string
                      afmObjKeyExpiration:
                        type: string
                      backgroundSpaceReclaimThreshold:
                        default: "15"
                        type: string
                      cipherList:
                        type: string
                      controlSetxattrImmutableSELinux:
                        type: string
                      encryptionKeyCacheExpiration:
                        type: string
                      enforceFilesetQuotaOnRoot:
                        type: string
                      ignorePrefetchLUNCount:
                        type: string
                      ignoreReplicaSpaceOnStat:
                        default: "yes"
                        enum:
                        - "yes"
                        - "no"
                        type: string
                      ignoreReplicationForQuota:
                        default: "yes"
                        enum:
                        - "yes"
                        - "no"
                        type: string
                      ignoreReplicationOnStatfs:
                        default: "yes"
                        enum:
                        - "yes"
                        - "no"
                        type: string
                      initPrefetchBuffers:
                        type: string
                      ioHangDetectorTimeout:
                        type: string
                      maxBufferDescs:
                        type: string
                      maxMBpS:
                        type: string
                      maxTcpConnsPerNodeConn:
                        type: string
                      maxblocksize:
                        type: string
                      nsdMaxWorkerThreads:
                        type: string
                      nsdMinWorkerThreads:
                        type: string
                      nsdMultiQueue:
                        type: string
                      nsdSmallThreadRatio:
                        type: string
                      nspdBufferMemPerQueue:
                        type: string
                      nspdQueues:
                        type: string
                      nspdThreadsPerQueue:
                        type: string
                      numaMemoryInterleave:
                        type: string
                      pagepoolMaxPhysMemPct:
                        type: string
                      panicOnIOHang:
                        type: string
                      pitWorkerThreadsPerNode:
                        type: string
                      prefetchPct:
                        type: string
                      prefetchThreads:
                        type: string
                      prefetchTimeout:
                        type: string
                      proactiveReconnect:
                        enum:
                        - "yes"
                        - "no"
                        type: string
                      qMaxBlockShare:
                        type: string
                      qRevokeDisable:
                        type: string
                      readReplicaPolicy:
                        default: local
                        enum:
                        - default
                        - local
                        - fastest
                        type: string
                      seqDiscardThreshold:
                        type: string
                      subnets:
                        type: string
                      tscCmdAllowRemoteConnections:
                        enum:
                        - "yes"
                        - "no"
                        type: string
                      tscCmdPortRange:
                        type: string
                      verbsPorts:
                        type: string
                      verbsRdma:
                        enum:
                        - enable
                        - disable
                        type: string
                      verbsRdmaCm:
                        enum:
                        - enable
                        - disable
                        type: string
                      verbsRdmaSend:
                        enum:
                        - "yes"
                        - "no"
                        type: string
                    type: object
                  fsMountPointPath:
                    description: |-
                      FSMountPointPath specifies the Filesystem mountpoint path
                      NOTE: only /mnt and /var/mnt are supported values and only apply to k8s cluster
                      Once set to cluster CR, this parameter is immutable and cannot be cleared. It can only be set once when previously unset.
                    enum:
                    - /mnt
                    - /var/mnt
                    type: string
                  hostAliases:
                    description: hostAliases that will be added to the internal DNS
                      that resolves hosts for core pods
                    items:
                      properties:
                        hostname:
                          description: Hostname for the associated IP address.
                          type: string
                        ip:
                          description: IP address of the host file entry.
                          type: string
                      required:
                      - hostname
                      - ip
                      type: object
                    type: array
                  nodeProfiles:
                    description: |-
                      nodeProfiles specifies IBM Storage Scale configuration parameters for a
                      subset of nodes, which overwrites parameters specified in "clusterProfile".
                      NodeProfiles also specifies memory and CPU resources for a subset of core
                      pods, which overwrites memory and CPU resources specified in "resources".
                    items:
                      properties:
                        name:
                          description: name is a unique name for the node profile.
                          maxLength: 64
                          pattern: ^[a-z0-9]([-a-z0-9]{0,61}[a-z0-9])?$
                          type: string
                          x-kubernetes-validations:
                          - message: Node profile name 'all' or a name that ends with
                              'nodes' is a reserved key word that is not allowed.
                            rule: self != 'all' && !self.endsWith('nodes')
                        nodeSelector:
                          description: nodeSelector selects the nodes that belong
                            to the node profile.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        profile:
                          description: profile specifies the node-scoped IBM Storage
                            Scale configuration parameters.
                          properties:
                            maxMBpS:
                              type: string
                            numaMemoryInterleave:
                              type: string
                            subnets:
                              type: string
                            verbsPorts:
                              type: string
                            verbsRdma:
                              enum:
                              - enable
                              - disable
                              type: string
                          type: object
                        resources:
                          description: resources defines the Memory/CPU resources
                            for the core pods that belong to the node profile.
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This field depends on the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                      required:
                      - name
                      - nodeSelector
                      type: object
                    maxItems: 50
                    type: array
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      nodeSelector will be applied to daemon core pods. The selectors in this field are ANDed.
                      This means that only nodes are selected which have all labels of this field.
                      This field is logically ANDed with any nodeSelectorExpressions also configured.
                    type: object
                  nodeSelectorExpressions:
                    description: nodeSelectorExpressions that will apply to daemon
                      core pods. This field is logically ANDed with any nodeSelector
                      also configured.
                    items:
                      description: |-
                        A node selector requirement is a selector that contains values, a key, and an operator
                        that relates the key and values.
                      properties:
                        key:
                          description: The label key that the selector applies to.
                          type: string
                        operator:
                          description: |-
                            Represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                          type: string
                        values:
                          description: |-
                            An array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. If the operator is Gt or Lt, the values
                            array must have a single element, which will be interpreted as an integer.
                            This array is replaced during a strategic merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  nsdDevicesConfig:
                    default:
                      bypassDiscovery: true
                    description: |-
                      nsdDevicesConfig allows users to specify non-standard device names in order to override or enhance the disk discovery process.
                      This parameter must only be specified when using a local filesystem with devices that use non-standard device names.
                    properties:
                      bypassDiscovery:
                        default: true
                        description: |-
                          bypassDiscovery allows bypass of automatic disk discovery. If set to true, only the set of devices
                          defined by the localDevicePaths will be used. If set to false, the automatic device discovery will find
                          devices with standard device names.
                        enum:
                        - true
                        - false
                        type: boolean
                      localDevicePaths:
                        description: localDevicePaths specifies the device names and
                          device types.
                        items:
                          properties:
                            devicePath:
                              description: |-
                                devicePath specifies nsd device names. Allows wildcards.
                                For example: '/dev/sdd', '/dev/pvc*', ...
                              type: string
                            deviceType:
                              default: generic
                              description: |-
                                deviceType specifies the device type.
                                For example: 'dmm', 'vpath', 'generic', ...
                              type: string
                          type: object
                        type: array
                    type: object
                  preserveNativeHostname:
                    description: preserveNativeHostname - Only change if adviced by
                      IBM.
                    type: boolean
                  quorum:
                    description: quorum specifies configuration for managing the quorum
                      nodes.
                    properties:
                      nodeDownTimeout:
                        default: 30
                        description: |-
                          nodeDownTimeout specifies the time in minutes when quorum is moved
                          away from nodes where Storage Scale is down. Storage Scale is down
                          if the core pod is not running or the Kubernetes node is down. The
                          default is 30 minutes. It is recommended to avoid specifying a value
                          below 20. Set the value to 0 to disable automatic quorum move from
                          nodes where Storage Scale is down.
                          This parameter is igoned on a 3 node cluster.
                        minimum: 0
                        type: integer
                      nodeSelector:
                        description: |-
                          nodeSelector selects the Kubernetes nodes that can act as quorum nodes.
                          From these selected nodes, depending of cluster size the operator
                          chooses 3, 5, 7 or 9 nodes as quorum nodes. If this node selector selects
                          less nodes, the operator selects additional nodes as quorum nodes which
                          are not selected by this selector.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      supportMaxUnavailable:
                        default: true
                        description: |-
                          supportMaxUnavailable allows to make one or multiple quorum nodes
                          unavailable. If set to "false", the operator never deletes more than
                          one quorum pod at the same time. By default this parameter is "true",
                          which allows the operator to delete multiple quorum pod in parallel.
                          Set this parameter to "false" if availability of the container native
                          cluster is more important than update speed.
                        type: boolean
                    type: object
                  resources:
                    description: |-
                      resources specifies the memory and CPU requests and limits for all core pods.
                      This can be overwritten for subsets of nodes by defining resources in node profiles.
                    nullable: true
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  roles:
                    description: |-
                      roles specify the IBM Storage Scale configuration parameters for nodes that apply to a role.
                      Specifying configuration parameters for roles is optional and does overwrite a set of default parameters.
                      roles is deprecated. Use nodeProfiles instead.
                    items:
                      properties:
                        limits:
                          description: The Memory/CPU resource limits that will be
                            set for Scale core pods.
                          properties:
                            cpu:
                              description: CPU is measured in cpu units (i.e 1, 2,
                                100m, 2500m)
                              maxLength: 20
                              type: string
                            memory:
                              description: |-
                                Memory is measured in bytes as plain integer or with kubernetes supported suffixes (i.e 128974848, 129e6, 129M, 123Mi).
                                The value is the maximum amount of memory the Scale core pod is allowed to consume.
                              maxLength: 20
                              type: string
                          type: object
                        name:
                          description: Name of the role. Only afm, storage or client
                            are allowed.
                          enum:
                          - afm
                          - storage
                          - client
                          type: string
                        profile:
                          description: |-
                            IBM Storage Scale node-scoped configuration parameters.
                            Changing these values is unsupported and should
                            not be changed unless advised by IBM Support
                          properties:
                            afmMaxParallelRecoveries:
                              type: string
                            backgroundSpaceReclaimThreshold:
                              default: "15"
                              type: string
                            controlSetxattrImmutableSELinux:
                              type: string
                            ignorePrefetchLUNCount:
                              type: string
                            initPrefetchBuffers:
                              type: string
                            ioHangDetectorTimeout:
                              type: string
                            maxBufferDescs:
                              type: string
                            maxMBpS:
                              type: string
                            maxTcpConnsPerNodeConn:
                              type: string
                            maxblocksize:
                              type: string
                            nsdMaxWorkerThreads:
                              type: string
                            nsdMinWorkerThreads:
                              type: string
                            nsdMultiQueue:
                              type: string
                            nsdSmallThreadRatio:
                              type: string
                            nspdBufferMemPerQueue:
                              type: string
                            nspdQueues:
                              type: string
                            nspdThreadsPerQueue:
                              type: string
                            numaMemoryInterleave:
                              type: string
                            pagepoolMaxPhysMemPct:
                              type: string
                            panicOnIOHang:
                              type: string
                            pitWorkerThreadsPerNode:
                              type: string
                            prefetchPct:
                              type: string
                            prefetchThreads:
                              type: string
                            prefetchTimeout:
                              type: string
                            proactiveReconnect:
                              enum:
                              - "yes"
                              - "no"
                              type: string
                            seqDiscardThreshold:
                              type: string
                            subnets:
                              type: string
                            tscCmdPortRange:
                              type: string
                            verbsPorts:
                              type: string
                            verbsRdma:
                              enum:
                              - enable
                              - disable
                              type: string
                            verbsRdmaCm:
                              enum:
                              - enable
                              - disable
                              type: string
                            verbsRdmaSend:
                              enum:
                              - "yes"
                              - "no"
                              type: string
                          type: object
                        resources:
                          description: The Memory/CPU resource requests that will
                            be set for Scale core pods.
                          properties:
                            cpu:
                              description: CPU is measured in cpu units (i.e 1, 2,
                                100m, 2500m)
                              maxLength: 20
                              type: string
                            memory:
                              description: |-
                                Memory is measured in bytes as plain integer or with kubernetes supported suffixes (i.e 128974848, 129e6, 129M, 123Mi).
                                The value is a target and will be requested for Scale core pods.
                                Resource request limits on containers impact pod scheduling and bin packing.
                              maxLength: 20
                              type: string
                          type: object
                      type: object
                      x-kubernetes-validations:
                      - message: CPU requests must be set when CPU limits are specified
                        rule: '!(has(self.limits.cpu) && self.limits.cpu.size() !=
                          0 && (!has(self.resources.cpu) || self.resources.cpu.size()
                          == 0))'
                      - message: Memory requests must be set when memory limits are
                          specified
                        rule: '!(has(self.limits.memory) && self.limits.memory.size()
                          != 0 && (!has(self.resources.memory) || self.resources.memory.size()
                          == 0))'
                      - message: CPU limits must be at least twice the CPU requests
                          value
                        rule: '!(has(self.limits) && has(self.limits.cpu) && self.limits.cpu.size()
                          != 0 && has(self.resources.cpu) && self.resources.cpu.size()
                          != 0 && quantity(self.limits.cpu).isLessThan(quantity(self.resources.cpu).add(quantity(self.resources.cpu))))'
                      - message: Memory limits must be at least twice the memory requests
                          value
                        rule: '!(has(self.limits) && has(self.limits.memory) && self.limits.memory.size()
                          != 0 && has(self.resources.memory) && self.resources.memory.size()
                          != 0 && quantity(self.limits.memory).isLessThan(quantity(self.resources.memory).add(quantity(self.resources.memory))))'
                      - message: Memory requests must be at least 2Gi (4Gi for non-s390x
                          architectures)
                        rule: '!(has(self.resources.memory) && self.resources.memory.size()
                          != 0 && quantity(self.resources.memory).isLessThan(quantity(''2Gi'')))'
                    maxItems: 20
                    type: array
                  storageClass:
                    description: |-
                      StorageClass specifies the external storage,
                      which is provisioned as a PersistentVolumeClaim (PVC) and used as the backup target for SDR/CCR data.
                      NOTE: this option should only be used when advised by IBM.
                    type: string
                  tolerations:
                    description: tolerations that are applied to daemon core pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    maxItems: 32
                    type: array
                    x-kubernetes-validations:
                    - message: 'Toleration with any hard effect: NoSchedule, NoExecute
                        must specify a key'
                      rule: self.all(tol, tol.effect == 'PreferNoSchedule' || tol.key.size()
                        != 0)
                    - message: 'Tolerating ''node.kubernetes.io/unschedulable'' with
                        any hard effect: NoSchedule, NoExecute is not allowed'
                      rule: self.all(tol, tol.effect == 'PreferNoSchedule' || tol.key
                        != 'node.kubernetes.io/unschedulable')
                  update:
                    description: update defines the update behavior of the Scale core
                      pods. If not specified, all core pods are updated one by one.
                    properties:
                      maxUnavailable:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          maxUnavailable defines either an integer number or percentage of core pods
                          and nodes that can go Unavailable during an update. This only affects core
                          pods that do not reside in any update pool (see `pools` parameter).
                          The default value is 1. A value larger than 1 will mean multiple core pods
                          and nodes going unavailable during the update, which causes that PV storage
                          of Storage Scale CSI is unavailable and may affect your workload stress on
                          the remaining nodes. You cannot set this value to 0 to stop updates;
                          to stop updates, use the 'paused' property instead.
                        x-kubernetes-int-or-string: true
                      paused:
                        default: false
                        description: |-
                          paused specifies whether or not updates should be stopped. This only affects
                          core pods that do not reside in any update pool (see `pools` parameter).
                        type: boolean
                      pools:
                        description: |-
                          pools describe a set of update pools. An update pool describes the update
                          behavior of selected core pods, for example how many pods can be updated in
                          parallel.
                        items:
                          properties:
                            maxUnavailable:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                maxUnavailable defines either an integer number or percentage of core pods
                                and nodes in the pool that can go Unavailable during an update.
                                The default value is 1. A value larger than 1 will mean multiple core pods
                                and nodes going unavailable during the update, which causes that PV storage
                                of Storage Scale CSI is unavailable and may affect your workload stress on
                                the remaining nodes. You cannot set this value to 0 to stop updates;
                                to stop updates, use the 'paused' property instead.
                                This parameter must not be specified if the update pool references to an
                                Openshift MachineConfigPool (means pool name has "mcp/" prefix).
                              x-kubernetes-int-or-string: true
                            name:
                              description: |-
                                name is the name of the update pool. This pool references to an OpenShift
                                MachineConfigPool if the name has a "mcp/" prefix (for example "mcp/worker").
                              type: string
                            nodeSelector:
                              description: |-
                                nodeSelector selects the Kubernetes nodes that host the core pods that belong
                                to this update pool. Nodes that do not host a core pod are ignored.
                                This parameter must not be specified if the update pool references to an
                                Openshift MachineConfigPool (means pool name has "mcp/" prefix).
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            paused:
                              default: false
                              description: |-
                                paused specifies whether or not updates to this update pool should be stopped.
                                This parameter is ignored if the update pool references to an Openshift
                                MachineConfigPool (means pool name has "mcp/" prefix).
                              type: boolean
                          required:
                          - name
                          type: object
                        type: array
                    type: object
                type: object
                x-kubernetes-validations:
                - message: fsMountPointPath may be set once when previously unset;
                    once set it is immutable and cannot be cleared
                  rule: '!(has(oldSelf.fsMountPointPath) && !has(self.fsMountPointPath))
                    && (!has(oldSelf.fsMountPointPath) || self.fsMountPointPath ==
                    oldSelf.fsMountPointPath)'
              debugConfig:
                description: |-
                  It holds configuration for added debugging functionality.
                  If defined, a daemonset is created to have pods running along side each core pod.
                properties:
                  threadDeadlockCapture:
                    description: threadDeadlockCapture, when enabled, will monitor
                      all active kernel threads to allow snapshots to be taken during
                      suspected deadlock scenarios.
                    type: boolean
                type: object
              gpfsModuleManagement:
                description: |-
                  moduleManagement specifies the way to build kernel modules.
                  The GPFS kernel modules can be built using kernel module management operator
                  or using machine config operator.
                properties:
                  kmm:
                    description: |-
                      kmm is configured to build kernel modules using Kernel
                      Module Management Operator.
                    properties:
                      imageRepository:
                        description: |-
                          ImageRepository defines the container image registry details used by the
                          Kernel Module Management Operator to push and pull built kernel module images.
                        properties:
                          registry:
                            description: "registry specifies the the container image
                              registry server\nFor example:\n\t1. \"quay.io\"\n\t2.
                              \"example-registry-quay-openshift-operators.apps.abc.com:5000\""
                            maxLength: 255
                            type: string
                            x-kubernetes-validations:
                            - messageExpression: '''Invalid value: "'' + self + ''":
                                length must be at most 255 characters'''
                              rule: self.size() <= 255
                            - messageExpression: '''Invalid value: "'' + self + ''":
                                Registry must be a valid FQDN or IP, optionally with
                                port (1-65535)'''
                              rule: self.matches('^(([a-zA-Z0-9]([a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\\.)+[a-zA-Z]([a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(:([1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5]))?|((25[0-5]|(2[0-4]|1\\d|[1-9]|)\\d)\\.){3}(25[0-5]|(2[0-4]|1\\d|[1-9]|)\\d)(:([1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5]))?)$')
                          registrySecret:
                            description: |-
                              registrySecret specifies the name of a Secret containing container
                              registry credentials used to authenticate when pushing or pulling
                              kernel module images.

                              The referenced Secret must exist in the same namespace and should
                              be of type `kubernetes.io/dockerconfigjson`.
                            type: string
                          repo:
                            description: "repo specifies the container image repository
                              inside the registry\nwhich will be used by the Kernel
                              Module Management Operator to package\nbuilt kernel
                              modules. The value must include repository path, and
                              image name. Do not include the tag. KMM adds the kernel
                              version as tag.\nFor example:\n\t1. \"my-org/gpfs-kmod\".
                              Here \"my-org\" is the repo path and \"gpfs-kmod\" is
                              the image name.\n\t2. my-org/test/gpfs-kmod.  Here \"my-org/test\"
                              is the repo path and \"gpfs-kmod\" is the image name."
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: repo must be set when registry is specified
                          rule: '!has(self.registry) || self.registry.size() == 0
                            || (has(self.repo) && self.repo.size() != 0)'
                        - message: registry must be set when repo or registrySecret
                            is specified
                          rule: ((!has(self.repo) || self.repo.size() == 0) && (!has(self.registrySecret)
                            || self.registrySecret.size() == 0)) || (has(self.registry)
                            && self.registry.size() != 0)
                        - message: repo must contain repository path and image name
                            separated by '/' (e.g., 'my-org/gpfs-kmod')
                          rule: '!has(self.repo) || self.repo.size() == 0 || (self.repo.contains(''/'')
                            && !self.repo.startsWith(''/'') && !self.repo.endsWith(''/''))'
                      moduleSigning:
                        description: |-
                          ModuleSigning specifies the names of Secrets containing signing keys.

                          Kernel Module Management uses these Secrets to sign kernel modules on cluster nodes
                          where secure boot is enabled. The Secrets must exist in the same namespace
                          and should contain valid signing keys.
                        properties:
                          certSecret:
                            description: |-
                              CertSecret is the name of a Secret containing the public Secure Boot key.
                              This certificate is used to verify kernel module signatures on cluster nodes.
                            type: string
                          keySecret:
                            description: |-
                              KeySecret is the name of a Secret containing the private Secure Boot key.
                              Kernel Module Management uses this key to sign kernel modules for nodes
                              with Secure Boot enabled.
                            type: string
                        type: object
                    type: object
                type: object
              grafanaBridge:
                description: It tells the operator how to configure Grafana Bridge.
                properties:
                  enablePrometheusExporter:
                    type: boolean
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: nodeSelector specifies the criteria used to determine
                      which nodes deploy grafana bridge pods.
                    type: object
                  tolerations:
                    description: tolerations is applied to grafana bridge pods
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              gui:
                default:
                  enableSessionIPCheck: true
                description: It tells the operator how to configure the GUIs.
                properties:
                  containerResources:
                    description: |-
                      containerResources allow to specify memory and CPU requests and limits for containers of GUI pods.
                      In large clusters with high number of PersistentVolumes the GUI containers require more resources than default values.
                    items:
                      properties:
                        name:
                          description: name is the container name.
                          enum:
                          - liberty
                          - postgres
                          type: string
                        resources:
                          description: resources defines the resources for this container
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This field depends on the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                      required:
                      - name
                      - resources
                      type: object
                    type: array
                  enableSessionIPCheck:
                    description: |-
                      enableSessionIPCheck verifies that all requests to GUI within the same session are send from the same client IP.
                      Enabling this flag prevents session hijacking. Without IP validation, if an attacker intercepts the session ID,
                      the attacker can potentially use the session ID from another IP address without being detected.
                      The client IP check can be disabled in case a load balancer causes that the requests to GUI within same session are send by different IPs.
                    type: boolean
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: nodeSelector that is applied to GUI pods
                    type: object
                  tolerations:
                    description: tolerations that are applied to GUI pods
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              license:
                description: The license must be accepted by the end user that provides
                  a way to specify the IBM Spectrum Scale Edition.
                properties:
                  accept:
                    description: |-
                      Read the license and specify "true" to accept or "false" to not accept.
                      https://www.ibm.com/support/customer/csol/terms/?id=L-YHGZ-JQ5CU3
                      BY DOWNLOADING, INSTALLING, COPYING, ACCESSING, CLICKING ON AN "ACCEPT" BUTTON, OR OTHERWISE USING THE PROGRAM, LICENSEE AGREES TO THE TERMS OF THIS AGREEMENT. IF YOU ARE ACCEPTING THESE TERMS ON BEHALF OF LICENSEE, YOU REPRESENT AND WARRANT THAT YOU HAVE FULL AUTHORITY TO BIND LICENSEE TO THESE TERMS. IF YOU DO NOT AGREE TO THESE TERMS,
                      * DO NOT DOWNLOAD, INSTALL, COPY, ACCESS, CLICK ON AN "ACCEPT" BUTTON, OR USE THE PROGRAM; AND
                      * PROMPTLY RETURN THE UNUSED MEDIA AND DOCUMENTATION TO THE PARTY FROM WHOM IT WAS OBTAINED FOR A REFUND OF THE AMOUNT PAID. IF THE PROGRAM WAS DOWNLOADED, DESTROY ALL COPIES OF THE PROGRAM.
                    enum:
                    - true
                    type: boolean
                  license:
                    description: It specifies the IBM Spectrum Scale edition, "data-access"
                      or "data-management".
                    enum:
                    - data-access
                    - data-management
                    type: string
                required:
                - accept
                - license
                type: object
              networkPolicy:
                description: It tells the operator how to configure NetworkPolicies
                type: object
              pmcollector:
                description: It tells the operator how to configure the pmcollectors.
                properties:
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: nodeSelector that is applied to pmcollector pods
                    type: object
                  pmcollectorContainerResources:
                    description: pmcollectorContainerResources allow to specify memory
                      and CPU requests and limits for containers of pmcollector pods.
                    items:
                      properties:
                        name:
                          description: name is the container name.
                          enum:
                          - sysmon
                          - pmcollector
                          type: string
                        resources:
                          description: resources defines the resources for this container
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This field depends on the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                      required:
                      - name
                      - resources
                      type: object
                    type: array
                  storageClass:
                    description: |-
                      storageClass is used for creating PVCs for pmcollector pods, if not specified default storage class 'ibm-spectrum-scale-internal' is used.
                      Modifying this field will delete existing PVCs and create new ones with the updated storage class. This leads to the loss of the historical performance data.
                    type: string
                  tolerations:
                    description: tolerations that are applied to pmcollector pods
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
            required:
            - license
            type: object
            x-kubernetes-validations:
            - message: nodeProfiles and roles cannot both be set at the same time;
                roles is deprecated.
              rule: '!(has(self.daemon.nodeProfiles) && self.daemon.nodeProfiles.size()
                > 0 && has(self.daemon.roles) && self.daemon.roles.size() > 0)'
            - message: nodeProfiles must have unique names.
              rule: '!has(self.daemon.nodeProfiles) || self.daemon.nodeProfiles.all(x,
                self.daemon.nodeProfiles.filter(y, y.name == x.name).size() == 1)'
          status:
            description: status defines the observed state of Cluster
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - message: The only supported name is ibm-spectrum-scale.
          rule: '!has(self.metadata.name) || self.metadata.name == "ibm-spectrum-scale"'
        - message: EnforceFilesetQuotaOnRoot parameter cannot be changed.
          rule: '!has(oldSelf.spec.daemon.clusterProfile.enforceFilesetQuotaOnRoot)
            || self.spec.daemon.clusterProfile.enforceFilesetQuotaOnRoot == oldSelf.spec.daemon.clusterProfile.enforceFilesetQuotaOnRoot'
        - message: '''ControlSetxattrImmutableSELinux'' parameter cannot be changed.'
          rule: '!has(oldSelf.spec.daemon.clusterProfile.controlSetxattrImmutableSELinux)
            || self.spec.daemon.clusterProfile.controlSetxattrImmutableSELinux ==
            oldSelf.spec.daemon.clusterProfile.controlSetxattrImmutableSELinux'
        - message: '''scale'' key cannot be changed.'
          rule: '!has(self.spec.csi) || !has(self.spec.csi.sidecar) || !has(self.spec.csi.sidecar.nodeSelector)
            || !(''scale'' in self.spec.csi.sidecar.nodeSelector)'
        - message: Invalid nodeSelectorExpressions format
          rule: '!has(self.spec.daemon.nodeSelectorExpressions) || self.spec.daemon.nodeSelectorExpressions.all(req,
            has(req.key) && has(req.operator) && (req.operator in [''In'', ''NotIn'',
            ''Exists'', ''DoesNotExist'', ''Gt'', ''Lt'']))'
        - message: afmObjKeyExpiration must be between 0 and 2147483647
          rule: '!has(self.spec.daemon.clusterProfile) || !has(self.spec.daemon.clusterProfile.afmObjKeyExpiration)
            || self.spec.daemon.clusterProfile.afmObjKeyExpiration.size() == 0 ||
            (int(self.spec.daemon.clusterProfile.afmObjKeyExpiration) >= 0 && int(self.spec.daemon.clusterProfile.afmObjKeyExpiration)
            <= 2147483647)'
        - message: Invalid cipher value. Must be 'AUTHONLY' or match pattern 'AES128-SHA',
            'AES128-SHA256', 'AES256-SHA', or 'AES256-SHA256'.
          rule: '!has(self.spec.daemon.clusterProfile) || !has(self.spec.daemon.clusterProfile.cipherList)
            || self.spec.daemon.clusterProfile.cipherList == ''AUTHONLY'' || self.spec.daemon.clusterProfile.cipherList.matches(''^AES(128|256)-SHA(256)?$'')'
        - message: Memory requests must be at least 2Gi unless the role already had
            memory requests defined in the previous version
          rule: '!has(self.spec.daemon.roles) || self.spec.daemon.roles.all(role,
            !has(role.resources.memory) || role.resources.memory.size() == 0 || quantity(role.resources.memory).isGreaterThan(quantity(''2Gi''))
            || quantity(role.resources.memory) == quantity(''2Gi'') || (has(oldSelf.spec.daemon.roles)
            && oldSelf.spec.daemon.roles.exists(oldRole, oldRole.name == role.name
            && has(oldRole.resources.memory) && oldRole.resources.memory.size() !=
            0)))'
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
