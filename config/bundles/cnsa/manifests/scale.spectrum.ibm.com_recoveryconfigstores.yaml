apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  creationTimestamp: null
  labels:
    app.kubernetes.io/instance: ibm-spectrum-scale
    app.kubernetes.io/name: operator
  name: recoveryconfigstores.scale.spectrum.ibm.com
spec:
  group: scale.spectrum.ibm.com
  names:
    kind: RecoveryConfigStore
    listKind: RecoveryConfigStoreList
    plural: recoveryconfigstores
    singular: recoveryconfigstore
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: RecoveryConfigStore is the Schema for the RecoveryConfigStore
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired configuration state
            properties:
              buckets:
                description: RecoveryConfigStoreBuckets defines the bucket configuration
                  used for storing and retrieving recovery configuration data
                properties:
                  local:
                    description: Local is the name of the local S3 bucket used for
                      storing config data
                    type: string
                  remote:
                    description: Remote is the name of the remote S3 bucket used as
                      a DR replica
                    type: string
                type: object
                x-kubernetes-validations:
                - message: local and remote buckets must be different
                  rule: self.local != self.remote
            type: object
          status:
            description: Status reflects the latest observed runtime state
            properties:
              conditions:
                items:
                  description: RecoveryConfigStoreCondition describes an individual
                    observed condition of the RecoveryConfigStore resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime records when the condition last
                        changed its status
                      format: date-time
                      type: string
                    status:
                      description: Status indicates whether the condition is True,
                        False, or Unknown
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: Type is the name of the condition
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
